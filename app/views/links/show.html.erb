<section class="link-page">
  <!-- Link Content -->
  <p class="link-paragraph">Title:</p>
  <h2><%= link_to @link.title, @link.url, :target => "_blank"  %></h2>
  <% if @link.user = current_user %>
  <%= link_to 'Edit', edit_link_path(@link), :class => "link-paragraph" %>
  <% end %>

  <p><%= link_to "Upvote", like_link_path(@link), method: :put %></p>
  <p>Link Score:<%= @link.score  %></p>
    <p><%= link_to "Lefty", leftvote_link_path(@link), method: :put %></p>
  <p>Political Points: <%= @link.political_points %></p>

  <hr>

  <!-- Comments -->
  <h3>Comments</h3>
  <% @comments.each do |comment| %>
    <div>
      <strong><%= comment.user_name %></strong>
      <br />
      <p><%= comment.body %></p>
      <% if comment.user_name = current_user %>
      <p><%= link_to 'Delete', comment_path(comment), method: :delete, data: { confirm: 'Are you sure?' } %></p>
      <% end %>
    </div>
  <% end %>
  <%= render 'comments/form' %>

</section>